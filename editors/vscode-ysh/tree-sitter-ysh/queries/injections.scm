; Injection queries for YSH
; These handle embedded languages like regex

; Eggex patterns could potentially inject regex highlighting
; (eggex) @injection.content
; (#set! injection.language "regex")

